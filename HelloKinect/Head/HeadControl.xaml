<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloKinect.Head">
    <Style TargetType="local:HeadControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:HeadControl">
                    <Canvas>
                        <Ellipse x:Name="PART_headPresenter"
                                 Canvas.Left="{TemplateBinding X}"
                                 Canvas.Top="{TemplateBinding Y}"
                                 Fill="Yellow" />
                    </Canvas>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
