<Page
    x:Class="HelloKinect.Main.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HelloKinect"
    xmlns:byteimage="using:HelloKinect.ByteImage"
    xmlns:head="using:HelloKinect.Head"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <byteimage:ByteImage ByteSource="{Binding ColourData}" 
               FrameWidth="1920"
               FrameHeight="1080" />

        <head:HeadControl X="{Binding HeadViewModel.X}"
                          Y="{Binding HeadViewModel.Y}" />

        <byteimage:ByteImage ByteSource="{Binding InfraredData}"
               Width="480"
               Height="270"
               FrameWidth="512"
               FrameHeight="424"            
               HorizontalAlignment="Left"
               VerticalAlignment="Top" />
        
        <Grid VerticalAlignment="Bottom">
            <Grid.Background>
                <SolidColorBrush Color="Black"
                                 Opacity="0.6" />
            </Grid.Background>

            <TextBlock Text="{Binding Greeting}"
                       TextAlignment="Center"
                       FontSize="56" />
        </Grid>
    </Grid>
</Page>