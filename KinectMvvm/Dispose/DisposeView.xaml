<framework:View
    x:Name="pageRoot"
    x:Class="KinectMvvm.Dispose.DisposeView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KinectMvvm.Dispose"
    xmlns:framework="using:KinectMvvm.Framework"
    xmlns:common="using:KinectMvvm.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Still need to implement IDisposable!"
                   TextAlignment="Center"
                   VerticalAlignment="Bottom"
                   FontSize="54" />
        
        <Button Grid.Row="1"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"
                FontSize="56"
                Command="{Binding GoBack}"
                Content="Go Back" />
    </Grid>
</framework:View>
